Install session created on `<source_channel>`.

Session: `<install_session_id>`
Status: `<status>`
Expires: `<expires_at>`

Install from this link:

<install_url>

If link-open is unavailable, run this on the target device (`<os>`):

```bash
<install_command>
```

I will keep polling install status and report completion.
