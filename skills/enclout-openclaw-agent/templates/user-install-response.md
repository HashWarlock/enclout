Install session created for connector `<connector_id>` on `<source_channel>`.

Session: `<install_session_id>`
Status: `<status>`
Expires: `<expires_at>`

Run this on the target device (`<os>`):

```bash
<install_command>
```

I will keep polling install status and report completion.
