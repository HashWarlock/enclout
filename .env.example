# Required: Bearer token for API authentication
ENCLOUT_AUTH_TOKEN=your-secret-token-here

# Required: Base64-encoded 32-byte Ed25519 signing seed
# Generate with: openssl rand -base64 32
ENCLOUT_SIGNING_KEY_B64=

# Optional: Connector ID for TEE identity registration on startup
ENCLOUT_CONNECTOR_ID=
